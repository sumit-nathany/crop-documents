# Claude project notes

Canonical context lives in `/CLAUDE.md` and `.cursor/rules/`. Prefer those over duplicating long docs here.

Priorities: Python CLI lab for deskew/rotation → iPhone app later → Mac GUI deferred.
